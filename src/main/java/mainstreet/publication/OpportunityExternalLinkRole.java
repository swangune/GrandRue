package mainstreet.publication;

/** Closed initial external-navigation role vocabulary for Opportunity v1. */
public enum OpportunityExternalLinkRole {
    OFFICIAL_APPLICATION,
    PROVIDER,
    SOURCE
}
