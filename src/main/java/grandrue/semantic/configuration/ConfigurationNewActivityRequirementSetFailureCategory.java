package grandrue.semantic.configuration;

/** Stable failure classification for durable requirement-set evidence. */
public enum ConfigurationNewActivityRequirementSetFailureCategory {
    VALIDATION_PACKAGE_EVIDENCE_NOT_FOUND,
    REQUIREMENT_RESOLUTION_FAILED,
    RESOLVED_PACKAGE_EVIDENCE_IDENTITY_CONFLICT,
    PERSISTENCE_FAILURE
}
